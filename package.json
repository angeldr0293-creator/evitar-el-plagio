{
  "name": "zerocopy-ia",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings server/server.js",
    "dev": "node --no-warnings server/server.js",
    "reset": "node --no-warnings server/utils/reset-data.js",
    "backup": "node --no-warnings server/utils/backup-data.js",
    "vite": "vite client",
    "build": "vite build client --outDir ../dist --emptyOutDir",
    "preview": "vite preview --outDir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
